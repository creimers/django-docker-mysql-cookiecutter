django:
  build: .

  environment:
    DJANGO_SETTINGS_MODULE: src.config.settings
    DB_HOST: postgres
    DB_NAME: postgres

  ports:
    - "8000:80"

  links:
    - postgres
  volumes:
    - ./src/:/project/src/

postgres:
  image: postgres
